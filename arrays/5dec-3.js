function find29WithindeOf(numbers) {
    return numbers.indexOf(29); // siempre que haya que devolver un valor ponemos return 
    // el 29 es el elemento a encontrar en el array
    // si pones (29,1) empieza a buscar el valor 29 desde la posicion 1
}
